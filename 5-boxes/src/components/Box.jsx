import { boxes } from '../shared/boxes';


const Box = () => {
    
    // boxes.map()
    const AA = boxes.map(item => <li> {item} </li>);

    console.log(AA);

    return (
        <>a</>
    )
}

export default Box; 